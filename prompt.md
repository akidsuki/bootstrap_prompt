このプロジェクトを、次の進め方で開発したい。まずその土台を作ってほしい。

1. 前提

以下は未確定。推測で埋めず、必要になった時点で私に聞くこと。

プロジェクトの目的とスコープ
言語・フレームワーク・実行基盤

決まっていること:
ドキュメント: 日本語
コメント: 日本語
Swagger/OpenAPI の description: 日本語
ログ・エラーメッセージ: 英語
コミットメッセージ: 英語

2. docs/ の骨組みを作る

docs/ に次の 10 フォルダを作り、それぞれに README.md（そのフォルダの書き方の規約。ルートの README.md とは別物）と template.md（雛形）を置く。個々の中身はまだ空でよい。

フォルダ	役割	問い
specs/	要件・仕様	何を作るか
adr/	決定記録	なぜそうするか
architecture/	構成	どう組み立てるか
conventions/	規約	どう書くか
planning/	計画	いつ・どの順で作るか
research/	調査	決める前に調べたこと
runbook/	手順書	決まった作業の手順
troubleshooting/	切り分け	症状から原因へ
journal/	作業記録	その日の経過
glossary/	用語集	語の定義

各 README には、役割・他フォルダとの使い分け・書き方のルールを書く。 次の 3 つは全フォルダ共通で入れること。

1 ファイル 100 行以内を目安にする（research だけ例外。調査は分量が要る）
理由を添える。 理由の無い規約は守られないし、状況が変わっても見直されない
決まっていないことは「未確定」と明記する。 埋めるために推測で書かない

併せて、プロジェクトルートに README.md を作成する。
利用者（このプロジェクトを使う人）に向けて必要なことを、その都度編集して育てていく。初期に決まった項目を並べて終わりにしない
100 行を超えたら、詳細を docs/ に移して README からリンクする。README は入口であって全体ではない
プロジェクトの目的・言語・フレームワークなど未確定の項目は、docs/ 同様に「未確定」と明記し、埋めない
詳細な設計判断やADRの内容は書かない。それは docs/ 側の役割
ライセンス・貢献方法（CONTRIBUTING相当）は初期では不要

併せて、docs/planning/roadmap.md を作成する。
再開時の起点になるファイルなので、docs/ の骨組みと同時に作る。
初期の中身は次の 3 見出しだけでよい。
  いま動くこと（初期は「未確定」）
  次にやること（初期は「未確定」）
  踏んだ落とし穴（初期は空）
CLAUDE.md の「再開時は planning/roadmap.md から読む」はこのファイルを指す

3. フォルダごとに必ず入れる規約

specs/

要件には ID を振る。REQ-<領域>-<連番>。テストからこの ID を参照し、要件と検証を対応づける。
  実装コードには書かない。1 関数が複数要件に関わるのが普通で、対応が曖昧になり、
  リファクタで追従されずに腐る
検証可能な形で書く。「高速に応答する」ではなく「p95 で 1 秒以内に応答する」。 検証方法が思いつかない要件は、まだ要件になっていない
MUST / SHOULD / MAY を使い分ける。SHOULD を外すときは理由を書く
実装方法は書かない。それは ADR か実装に属する
実態が変わったら書き換える。specs は常に「今あるべき姿」を指す

adr/

連番で採番し、状態は 提案 / 承認 / 却下 / 非推奨 / 置き換え済み
一度承認した ADR は書き換えない。 決定が変わったら新しい ADR を書き、古い方を 「置き換え済み（→ NNNN）」にして双方からリンクする
決定が覆った事実そのものが記録として価値を持つ

architecture/

図は Mermaid で書く。画像を貼らない。テキストなら差分が読める
図だけで終わらせない。責務の境界と「なぜそこで分けたか」は文で書く
コードを読めば分かることは書かない。複数ファイルを読まないと見えない関係だけ
外部との境界を必ず書く。 自分たちの管理下にないものとの境目が障害の入口になる

conventions/

ツールで強制できるものは文書に書かない。 formatter と linter の設定に置く。 文書に書くのは、ツールでは判定できない取り決めだけ
良い例だけでなく、避けたい書き方も併記する
後から作らない。 「そろえた方がよい」と気付いた時点で書く
Webサーバを立てる場合、Swagger/OpenAPI定義を必須とする。
  ただし、コーディングエージェントが内部検証や動作確認のために一時的に立てる
  サーバー（本番稼働もクライアントからの利用も想定しないもの）は対象外とする。
  理由: エンドポイントの形が外部から見えるようになった時点で、それが
  「対外的なインターフェース」か「使い捨ての検証コード」かは自動判定できない。
  判断が必要なので規約として明記する
Swagger/OpenAPI定義には、各エンドポイント・各パラメータ・各レスポンスフィールドに
  description を書く。型名だけでは「何のための値か」「制約」「省略時の挙動」が
  分からない。
  description の有無は Spectral 等の OpenAPI linter で検査し、CI で落とす。
  文書に残すのはこの方針と理由のみ。linter の選定と設定は言語・フレームワークが
  決まってから行い、それまでは「導入予定」として残す
コメントは最小限にする。コードから読み取れることは書かない。
  コメントを書くのは「なぜそうしたか」「コードだけでは読み取れない制約や意図」
  がある場合に限る。自明な処理へのコメントは書かない
  ただし、テストにおける要件 ID（REQ-<領域>-<連番>）の参照はこの規約の対象外とし、
  必ず書く。要件とテストの対応はコードからは読み取れない
秘密情報（API キー、パスワード、接続文字列など）はコミットしない。
  .env は .gitignore に入れ、必要な変数名だけを .env.example に置く。
  最初のコミットの前に決めないと、履歴に残ってから消すのは困難

planning/

完了条件を書く。「X を実装する」ではなく「何が満たされたら終わりか」。 書けない作業は、まだ分解が足りていない
依存を明示する。着手してから気付くと待ち時間になる
見積もりは書かなくてよい。順序と依存が分かれば足りる

research/

調査日を必ず書く。 日付の無い調査結果は、古いのか新しいのか判断できない
出典を書く。一次情報と、二次情報や推測を区別する
事実と解釈を分けて書く
結論が出なかったことを書く。 そこが次の調査の起点になる

runbook/

1 手順 1 動作。1 つの番号に複数の操作を詰めない
各手順に確認方法を添える。 何を見れば成功と判断できるかを書く
判断が入る箇所は分岐として書く。「状況に応じて」で済ませない
取り返しのつかない操作は明示する
最後に実行できた日付を残す

troubleshooting/

ファイル名と見出しは症状で書く。 原因名で書かない。読む人が知っているのは症状だけ
切り分けは、速く安く判定できるものから並べる
正常時の値を併記する。 「この値なら正常」が無いと、見ても判断できない
該当しなかった可能性も残す。次に同じ症状を追う人の時間を節約する
応急処置と恒久対応を区別する。恒久対応が未着手ならそう書く
未解決のまま残してよい

journal/

YYYY-MM-DD.md で 1 日 1 ファイル
過去の記録を書き換えない。 訂正は新しい日付で書く
事実と、その時点の推測を分けて書く
うまくいかなかったことを書く。 失敗と手戻りこそ後から効く
journal は書いた時点の記録であって現在の姿ではない。 仕様・構成・規約の根拠に使わない

glossary/

ここに無い語を仕様で使わない
1 語 1 エントリ。定義は 1〜2 文
英語表記と略語を併記する。識別子名と列名の根拠になる
同義語を残さない。どちらかを正とし、もう一方は「→ 正の語」とだけ書く
曖昧なまま載せない。定義が書けない語は、まだ理解が固まっていない

4. CLAUDE.md を書く

作った規約を CLAUDE.md にまとめる。次の表を必ず含めること。ここが一番効く。

開発の途中で以下が発生したら、その場で対応するドキュメントを書く。 後からまとめて書き起こさない。判断の理由はその場でしか残らない。

発生したこと	書く場所
選択肢があり、片方を選んだ	adr/ に 1 件
選ぶ前に調べた	research/ に残し、ADR からリンク
本番コードに入る依存ライブラリを足す	adr/ に 1 件（下記の確認項目を含める）
振る舞いが決まった	specs/ を更新
構成要素や境界が決まった	architecture/ を更新
新しいドメイン用語を使った	glossary/ に定義してから使う
書き方を揃えたくなった	conventions/ に 1 件
作業の順序と依存が見えた	planning/ を更新
手順が定型化した	runbook/ に 1 件
原因不明の症状を追った	troubleshooting/ に、未解決でも残す
その日の試行錯誤	journal/ に追記（過去は書き換えない）
利用者のセットアップ・使い方・前提に影響する変更があった	README.md を更新

CLAUDE.md には次も書く。

作業を再開するときは planning/roadmap.md から読む。 いまどこまで動くか、次に何を するか、踏んだ落とし穴が 1 か所にまとまっている状態を保つ
実装の前に、関係する specs/ と architecture/ を読む。 書かれていないことを推測で 補わない。未確定なら、その場で確認するか「未確定」として残す
区切りがついた時点で、その都度コミットする。 1 コミット 1 目的。決定とその実装と ドキュメントの更新は同じコミットに入れてよいが、目的の違う変更は分ける
コミットメッセージは Conventional Commits の形式（feat: / fix: / docs: / refactor: / test: / chore:）で書く。変更の種類が grep で引ける
本番コードに入る依存を足す前に、次を確認し、結果を adr/ に残す。テスト用・開発用の依存は対象外
  標準ライブラリや既存の依存で済まないか
  他の候補を 2 つ以上挙げ、選ばなかった理由を書く
  最終リリース日と、issue への対応状況（放置されていないか）
  ライセンスがプロジェクトの利用形態と合うか
  推移的な依存がどれだけ増えるか
  今この機能が要るか。将来使うかもしれない、では足さない
関数・ファイル・フォルダの大きさの目安を書く。数値は言語が決まった時点で私に確認する（初期は「未確定」）。linter で検査できるものは linter に入れる

5. 私とのやり取り

要件が未確定のうちは、推測でエンティティやユースケースを作らない。 「未確定」と書く方が、埋めるより有用
「レビューして」と言ったら、指摘は動作に影響するものから並べる。 各指摘は、報告の前に自分でコードを読んで裏取りする
直したら、直ったことを実際に確かめてから報告する。 確かめていない部分は「未確認」と明示する
作業を完了として報告する前に、次を確認する。未達の項目があれば「未確認」「未実施」と明示する
  テストが通る
  linter が通る
  関係する docs/ を更新した
  planning/roadmap.md を更新した
取り返しのつかない操作は、実行前に私に確認する。
  git push --force、履歴の書き換え、ブランチやファイルの削除、本番相当の環境への変更、など
日付は date コマンド等で実際に取得する。推測で書かない。
  journal/ と research/ は日付が要で、推測の日付は記録の価値を壊す
specs/、architecture/、実装が食い違ったら、specs/ を正とする。
  ただし specs/ が古い可能性もあるので、直す前に私に聞く。勝手にどちらかへ寄せない
一括置換で名前を揃えるときは、置換後にその語が重なっていないか確かめる。 置換前の語が担っていた区別が、置換でちょうど消えることがある

まず 2 と 4 を作って、できたところで見せてほしい。3 の規約を docs/ 各フォルダの README に書き込むのはそのあとでよい
